# TDD + BDD: a primer

The repo contains a mock-up use case of a university enrollment api system developed applying tdd and som (not
religiously) ddd/bdd.

Each commit represent, more or less,  a green red yellow steps